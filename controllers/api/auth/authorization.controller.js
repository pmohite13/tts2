
// var express = require('express');
// var router = express.Router();
// var jwt = require('jsonwebtoken');
// var jwtSimple = require('jwt-simple');
// var bcrypt = require('bcryptjs');

// var config = require(`../../../lib/configLoader`);
// var VerifyToken = require('../../../lib/VerifyToken');

// var bodyParser = require('body-parser');

// router.use(bodyParser.urlencoded({ extended: false }));
// router.use(bodyParser.json());
// // var User = require('../../../models/user');
// var sendEmail = require('../../../lib/businessService');
// const host = process.env.HOST || 'localhost:3000';
// const port = process.env.PORT || 3000;

// var rand;



class AuthorizationController {

    constructor(router) {
        // router.get('/user', VerifyToken, this.getUser.bind(this));
        // router.post('/register', this.registerUser.bind(this));
        // router.post('/login', this.loginUser.bind(this));
        // router.post('/verify', this.verifyUser.bind(this));
        // router.post('/forgotPassword', this.forgotPassword.bind(this));
        // router.post('/resetPassword', this.resetPassword.bind(this));
    }  

}

module.exports = AuthorizationController;

